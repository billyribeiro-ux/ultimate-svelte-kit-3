node_modules
/build
/.svelte-kit
.env

# Databases. `*.db` covers `local.db` and the throwaway `e2e.db`; the `-shm` and
# `-wal` siblings exist because the connection runs in WAL mode.
*.db
*.db-journal
*.db-shm
*.db-wal

# End-to-end artefacts.
/test-results
/playwright-report

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
